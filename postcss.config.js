module.exports = {
    plugins: {
    	"autoprefixer": {},
   //      "postcss-pxtorem": {
			// rootValue: 100, 					// 换算基数，1rem = 100px
			// unitPrecision: 5,					// px转rem精确到小数点后几位
			// propList: ['*'],					// 指定转换成rem的属性，支持通配符 *
			// selectorBlackList: ['mint-'],			// str/reg 指定不转换的选择器，str时包含字段即匹配
			// replace: true,
			// mediaQuery: false,				// 媒体查询中的px是否转换
			// minPixelValue: 2					// 小于指定数值的px不转换
	  //   }	
    }
}